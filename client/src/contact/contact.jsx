import { Button } from "@mui/material";

const Contact = () => {
  return (
    <div>
      <div>
        <Button variant="contained">Logout</Button>
      </div>
    </div>
  );
};

export default Contact;
